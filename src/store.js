import Vue from 'vue';
import Vuex from 'vuex';

const state = { };
const mutations = { };
const actions = { };
const getter = { };

Vue.use(Vuex);

const store = new Vuex.Store({
  state,
  mutations,
  actions,
  getter,
});

export default store;
